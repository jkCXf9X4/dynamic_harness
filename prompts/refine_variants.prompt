Generate 3 refined variant system prompts by combining the strengths of the
WINNING prompts below, which were measured as the top performers.

Winning prompts:
[INSERT WINNING PROMPT TEXTS HERE]

1) Read each winning prompt; extract its strongest, non-redundant ideas.
MAKE LARGER ALTERATIONS: Do not merely blend or lightly edit the winning
prompts. Rebuild each variant with substantially different structure — reorder
and merge sections, change emphasis and priorities, condense or expand prose,
and reconsider overall length. Each variant should be a genuinely distinct
rewrite informed by the winners, not a near-copy. Bold, opinionated structural
changes are strongly preferred over conservative blending.
2) Create 3 new complete, self-contained system prompts that combine those
   strengths. PRESERVE AND STRENGTHEN every winning prompt's guidance on the
   `prune`/`restore` tools: instruct the agent to actively `prune` turns whose
   results are already written to disk (especially across long multi-file
   tasks) and to `restore` a pruned turn only when its content is needed again.
   Do not let this context-management guidance get diluted in the combination —
   it is essential for the "manyfiles" task's token-efficiency score. Each
   roughly 1000-1800 characters — do NOT obsess over an exact
   count; a correct, complete prompt beats one truncated to hit a limit.
3) Write ALL 3 variants to .optimize_benchmarks/variants_round2.json as a JSON
   object: {"n1": "<full text>", "n2": "<full text>", "n3": "<full text>"}
4) Verify the JSON parses and contains exactly 3 variants.

Do NOT run any benchmark tasks yourself. Do NOT evaluate or rank anything.
Only read, combine, generate, and write the JSON file.
Report with artifact .optimize_benchmarks/variants_round2.json.
